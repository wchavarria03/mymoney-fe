export default {
    defaultPath: '/dashboard/default',
    basename: '/', // only at build time to set, like /datta-able
    layout: 'vertical', // vertical, horizontal (not available in lite version)
    preLayout: null, // (not available in lite version)
    collapseMenu: false, // mini-menu
    XlayoutType: 'menu-light', // menu-dark, (menu-light, dark are not available in lite version)

    XnavIconColor: false,
    XheaderBackColor: 'header-default', // header-default, (header-blue, header-red, header-purple, header-lightblue, header-dark are not available in lite version)
    XnavBackColor: 'navbar-default', // navbar-default, (navbar-blue, navbar-red, navbar-purple, navbar-lightblue, navbar-dark are not available in lite version)
    XnavBrandColor: 'brand-default', // brand-default, (brand-blue, brand-red, brand-purple, brand-lightblue, brand-dark are not available in lite version)
    XnavBackImage: false, // not available in lite version
    XrtlLayout: false, // not available in lite version
    XnavFixedLayout: true,
    XheaderFixedLayout: false, // not available in lite version
    Xboxlayout: false,
    XnavDropdownIcon: 'style1', // style1, (style2, style3 are not available in lite version)
    XnavListIcon: 'style1', // style1, (style2, style3, style4, style5, style6 are not available in lite version)
    XnavActiveListColor: 'active-default', // active-default, (active-blue, active-red, active-purple, active-lightblue, active-dark are not available in lite version)
    XnavListTitleColor: 'title-default', // title-default, (title-blue, title-red, title-purple, title-lightblue, title-dark are not available in lite version)
    XnavListTitleHide: false, // not available in lite version
    XconfigBlock: false, // not available in lite version
    Xlayout6Background : 'linear-gradient(to right, #A445B2 0%, #D41872 52%, #FF0066 100%)', // used only for pre-layout = layout-6
    Xlayout6BackSize : '', // used only for pre-layout = layout-6
};
